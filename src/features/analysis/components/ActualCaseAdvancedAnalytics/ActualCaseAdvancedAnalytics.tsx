import React from "react";
import { Typography } from "shared/components/ui";

export const ActualCaseAdvancedAnalytics = () => {
  return (
    <>
      {[0, 1, 2, 3].map(() => (
        <Typography> advanced graphics</Typography>
      ))}
    </>
  );
};
